根据以下数据库表结构（DDL），将自然语言问题转换为正确的 SQL 查询。
- 仅生成 SELECT 查询，不生成 INSERT、UPDATE、DELETE 或 DDL 语句。
- 如果问题涉及数据修改或无法用现有表结构回答，返回："不支持该操作" 或 "现有表结构不支持回答该问题"。
- 确保 SQL 语法正确，符合目标数据库（MySQL）的规范。
- 仅返回纯 SQL 语句，不要换行，不要包含解释或额外文本。

### 问题
{question}

### 表结构（DDL）
{ddl}